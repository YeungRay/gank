<view class="container">
    <loading hidden="{{hidden}}">
    加载中...
    </loading>

    <view class="swiper-tab">
        <view class="swiper-tab-list {{currentTab==0?'on':''}}" data-current="0" bindtap="switchNav">Android</view>
        <view class="swiper-tab-list {{currentTab==1?'on':''}}" data-current="1" bindtap="switchNav">福利</view> 
    </view>

    <swiper current="{{currentTab}}" bindchange="swiperChange" class="swiper-box" style="height:{{winHeight - 40}}px">

        <swiper-item>

            <scroll-view class="hot-box" scroll-y="true">
           
        
            </scroll-view>
            

        </swiper-item>

        <swiper-item>

            <scroll-view class="hot-box" scroll-y="true">
           
            
            </scroll-view>
      
        </swiper-item>

    </swiper>

</view> 